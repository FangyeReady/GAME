using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class SpChange : MonoBehaviour {

    private int index = 4002;
    public Image image;
    private void OnGUI()
    {
        if (GUI.Button(new Rect(50, 50, 200, 100), "Click"))
        {
            
        }
    }
}
